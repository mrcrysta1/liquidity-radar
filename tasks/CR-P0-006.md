TASK ID: CR-P0-006
PHASE: P0
PRIORITY: HIGH
AGENT ID: OC-LEAD
AGENT NAME: OpenCode
AUTHORIZED ROLE: Lead Architect + System Implementation
PROJECT: LIQUIDITY-RADAR
EXPECTED OUTPUT: One-click Windows multi-agent launcher (START-AGENTS.bat) + shutdown option, with Node-based testable core logic
---
OBJECTIVE:
Replace the inspection-only startup\START-AGENTS.bat with a working one-click launcher that
starts the three Phase 0 agents (OpenCode OC-LEAD, Cline CL-UI, Aider AI-REF) in separate
windows against the repository root, while preserving Phase 0 governance: repository/branch
state checks, identity validation, and master-branch warning. The launcher must offer a
simple "start everything" flow, resolve agent binaries statically (no interactive prompts),
and avoid re-running already-running agents.

SCOPE:
- startup\START-AGENTS.bat — thin launcher wrapper
- startup\agent-env.mjs — testable Node core (identity map, binary detection, per-agent
  command builders, preflight, git info, single-instance guard)
- scripts\startup.test.mjs — tests for the launcher core
- docs\MULTI-AGENT-STARTUP.md — owner documentation
- agents\identities.json — read-only input (do not modify)
- scripts\agent-os.mjs — reused for identity/task validation where appropriate
- tasks\CR-P0-006.md + tasks\index.json — governance registration
- AGENTS.md — minimal update to Phase 0 section 4 (startup protocol only)

DO NOT:
- Touch any application source (React, engine, services, API, CSS, charts, trading logic)
- Check out, push, or merge branches from the launcher; it must remain inspect-only re Git
- Change the Aider invocation authored for this system
  (aider --model openrouter/openrouter/free --yes-always); do not reintroduce .aider.conf.yml
- Invent CLI invocations for agents; every command must be verified against the installed CLI
- Introduce new dependencies
- Auto-commit from the launcher or auto-close/lobotomize any agent

DEPENDENCIES:
- CR-P0-001 (Phase 0 Agent Operating System; identities + validator scripts)

REQUIRED VERIFICATION:
- node scripts\agent-os.test.mjs (must keep passing)
- node scripts\startup.test.mjs (new: binaries, command builders, lock guard, no-Git-mutation)
- npm run lint and npm run build (app unaffected, must still pass)
- startup\agent-env.mjs preflight dry-run from the repo root
- Conventional single commit; push task branch; open PR to master; do NOT merge

Branch: task/CR-P0-006-multi-agent-startup
Commit: one conventional commit per task
PR: dedicated branch -> master (do not merge self)