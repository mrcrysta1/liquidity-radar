TASK ID: CR-P0-009
PHASE: 0
PRIORITY: HIGH
AGENT ID: CL-UI
AGENT NAME: Cline
AUTHORIZED ROLE: UI/UX + Frontend Implementation
PROJECT: Liquidity Radar — Agent OS Control Center
EXPECTED OUTPUT: A polished Agent OS desktop/browser Control Center that launches with the existing one-click startup system and continuously shows agent status, tasks, and project activity.

OBJECTIVE: Build a modern "AI Agent Operating System" control-center interface (glass/translucent, adjustable opacity, compact/expand, always-on floating in browser mode) that shows OpenCode/Cline/Aider status (BUSY/REST/BLOCKED/UNKNOWN), task info, and project overview, plus a task activity list. Reuse the existing Agent Status Monitor as the single source of truth. Add startup integration and a browser/desktop mode without introducing Electron/Tauri.

SCOPE:
- New agent-os/ directory: local Node stdlib control-center server, public dashboard UI (HTML/CSS/JS), tests, README.
- New startup/START-DASHBOARD.bat one-click launcher; minimal additive launch of the dashboard in startup/START-AGENTS.bat.
- Reuse agent-status/agent-status.mjs analyze()/loadTaskRegistry() as source of truth (no duplicated status logic).
- Task bookkeeping: register CR-P0-009 in tasks/index.json; add board row; update agent-status/status.json for CL-UI truthfully.

DO NOT:
- Do NOT modify trading/crypto functionality, charts, market APIs, engine, or production business logic.
- Do NOT introduce Electron/Tauri or new runtime dependencies.
- Do NOT overwrite another agent's uncommitted work (e.g. smoke.mjs, aider-autonomy-test.txt).
- Do NOT claim tests/builds/production results that were not actually executed.

DEPENDENCIES:
- CR-P0-008 (Agent Status Monitor) — merged; provides analyze()/loadTaskRegistry().
- CR-P0-006 (multi-agent launcher) — provides startup/ pattern reused for the dashboard launcher.

REQUIRED VERIFICATION:
- node agent-os/control-center.test.mjs (dashboard unit + server integration)
- node agent-status/agent-status.test.mjs
- node scripts/agent-os.test.mjs
- node scripts/startup.test.mjs
- node agent-status/agent-status.mjs validate
- Frontend lint + production build when deployable React files change; otherwise confirm no app files changed.
- Live /api/state served over HTTP on 127.0.0.1.