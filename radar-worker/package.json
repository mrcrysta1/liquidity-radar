{
  "name": "radar-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Always-on collector for Liquidity Radar: whale-order history into Postgres.",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
